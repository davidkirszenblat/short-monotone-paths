# short-monotone-paths

Numerical check of the construction in *Short Monotone Paths for
Every Linear Program*: builds the monotone path from the apex to the
optimum for random linear programs and checks the edge bound,
lexicographic monotonicity, and that the path visits only vertices
of the polyhedron.

Requires numpy and scipy.

    python check.py
